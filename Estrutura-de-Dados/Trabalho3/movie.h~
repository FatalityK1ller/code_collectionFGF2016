#define TAM 2
#define structTAM 30
/*
* Como é uma pilha, à lista de filmes precisa ter um topo de execução. jefferson
*/
typedef struct /*filmes*/{
	int topo;
	char name[structTAM];
	float rating;
	char style[structTAM];
	char actor[structTAM];
	int year;
}movie;

void dados_todos(movie *c);

void dados_pilha(movie *c);

void menu(movie *c);

void menu(movie *c);

void imprime_topo(movie *c);
/*
* As informações nas funções de ordenar no menu, ainda precisão ser melhoradas para o código funcionar corretamente.
* quando continuar o código no switch/case é só apagar '/*' e continuar ajustando o código fonte.
*/

void listarFilmes(movie *c); // Esse erro eu quase não acho também, no nome listarFilmes(movie *c), estava faltando o 's' em filmes.

/*Inicio da implementação de exclusão de um elemento da pilha*/
void retira(movie *c);
