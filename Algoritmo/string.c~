#include <stdio.h>
#include <string.h>

typedef struct Conta{
	char s1[]={"faculdade integrada da grade fortaleza"};
	char s2[]={"faculdade"};
	int n;
}conta;

conta * str(conta *st){

	int i,j;
	int n = 0;

	for(i = 1; i < strlen(st->s1); i++){
         	for (j = 0; j < strlen(st->s2); j++){
                       
              		if(strcmp(sr->s1,st->s2) > 0){
                		strcpy(n,st->s1);
                 		strcpy(st->s1,st->s2);
                 		strcpy(st->s2,n);
				st++;
			}                             
           	}
	}
	
	printf("O caracter aparece %d vezes\n",n);       
      

return st;
}

int main(){

	conta a[1];
	conta *x=a;

	str(a); 

return 0;

}
